import { styled } from "lib/goober"
import { theme } from "utils/theme"

const Layout = styled.div`
  max-width: ${theme("space.x11")};
  width: 100%;
`

export const Experience = () => <Layout></Layout>
